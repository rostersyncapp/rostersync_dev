name: Supabase to Backblaze Backup

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install PostgreSQL Tools and Socat
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client socat

      - name: Start IPv6 Bridge
        # This runs in the background (&) and bridges IPv4 127.0.0.1 to Supabase IPv6
        run: |
          socat TCP4-LISTEN:5432,fork,reuseaddr TCP6:db.rddqcxfalrlmlvirjlca.supabase.co:5432 &
          sleep 5 # Give the bridge a moment to initialize

      - name: Dump and Compress Database
        env:
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          # We now point to 127.0.0.1 because socat is listening there
          pg_dump -h 127.0.0.1 \
                  -p 5432 \
                  -U postgres \
                  -d postgres > backup.sql
          gzip backup.sql

      - name: Upload to Backblaze B2
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.B2_APPLICATION_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.B2_APPLICATION_KEY }}
          AWS_DEFAULT_REGION: us-west-004 
        run: |
          FILENAME="rostersync_db_$(date +'%Y-%m-%d_%H-%M').sql.gz"
          aws s3 cp backup.sql.gz "s3://${{ secrets.B2_BUCKET_NAME }}/$FILENAME" \
            --endpoint-url "${{ secrets.B2_ENDPOINT }}"
